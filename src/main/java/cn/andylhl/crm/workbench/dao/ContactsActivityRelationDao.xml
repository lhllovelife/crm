<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.andylhl.crm.workbench.dao.ContactsActivityRelationDao">

    <!--保存联系人市场活动信息
    int save(ContactsActivityRelation contactsActivityRelation);-->
    <insert id="save" parameterType="ContactsActivityRelation">
        insert into tbl_contacts_activity_relation
        (
            id,
            contactsId,
            activityId
        )
        values
        (
            #{id},
            #{contactsId},
            #{activityId}
        )
    </insert>

</mapper>